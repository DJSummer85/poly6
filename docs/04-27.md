## Mai napra feladatok:

1. **CommandCenter layout redesign** ✅

### Változtatások:

- **Connection banner**: Kompakt lett, 70%-kal kevesebb helyet foglal
- **Számla információ panel**: CollapsiblePanel-be zárva (BalanceCard + AggregatePortfolioCard), alapból összecsukva
- **BotSelector**: Áthelyezve a main grid bal oldalára, a QuickTradePanel fölé
- **QuickTradePanel**: Most a BotSelector alatt van, ugyanabban az oszlopban
- **Statisztikák és előzmények**: CollapsiblePanel-be zárva (StatsBar + MarketHistory), alapból összecsukva
- **CompactDataBar**: Mindig látható marad, kiemelt fontosságú market data

### Új layout struktúra:

```
Connection Banner (kompakt)
▼ Számla információ (collapsible, default: closed)
CompactDataBar (always visible)
[BotSelector + QuickTrade] [Chart] [Activity]
▼ Statisztikák és előzmények (collapsible, default: closed)
```

### Fájlok:
- `frontend/src/components/dashboard/command-center.tsx` - átstrukturálva
- `frontend/src/components/ui/collapsible-panel.tsx` - új komponens (már korábban létrehozva) 