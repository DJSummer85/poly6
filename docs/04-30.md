 Next steps (prioritized):

  1. Verify credential flow works after re-login — Test that /user/balance returns real balance data and System Health shows "configured"
  2. Add winning_trades/losing_trades to aggregate hook type — The backend already returns these fields but the frontend type omits them, so
  StrategyPerformance estimates from win_rate unnecessarily
  3. Bootstrap bot portfolio data — bot_portfolios table is empty. When bots start trading, portfolios will auto-create, but currently the
  StrategyPerformance shows "Még nincsenek stratégiák" because there's zero trade history
  4. SSE bot event broadcaster verification — The orchestrator's bot_event_broadcaster sends events only for running bots. Since all bots are
  stopped, the live activity feed will be empty. This is correct behavior but worth confirming after starting a bot
  5. Consider: Add initial portfolio rows — Either auto-create bot_portfolios when bots are created, or run a migration to backfill them so bots
  show with $100 starting balance even before first trade


  Átnéztem read-only módban. Fontos: a munkafa dirty állapotban van, így az eredmények a jelenlegi helyi módosításokra vonatkoznak.

Validáció

cargo test --manifest-path backend/Cargo.toml: sikeres, 25 + 8 teszt átment.
cargo build --release --manifest-path backend/Cargo.toml: sikeres, de van Rust warning.
cd frontend && bun run build: sikeres.
cd frontend && bun run lint: sikeres exit code, de 15 Biome warning.
cargo clippy --all-targets -- -D warnings: sikertelen, 36+ lint error.
GitHub: nincs nyitott issue, nincs nyitott PR, nincs listázott workflow run.
Fő hibák

Backend Clippy piros: főleg redundant field names, too_many_arguments, should_implement_trait, map_or egyszerűsítések, get(0) helyett first(), stb.
Backend warning: backend/src/api/sse.rs (line 513) current_api_latency kezdőértéke sosem olvasódik.
Frontend lint warningok: frontend/src/hooks/use-wallet.ts (line 44) és frontend/app/funding/page.tsx (line 79) több any MetaMask/provider/error kezelésnél.
Valós működési kockázat: backend/src/trading/orchestrator.rs (line 659) minden trade döntést logol, majd paper módban ugyanazt újra logolja (line 807). Ez duplikált paper trade decision rekordokat okozhat.
Live order sizing ellenőrizendő: orchestrator (line 950) size: bet_size értéket ad át, míg quick trade oldalon amount / trade_price (line 611) szerepel. Ezt teszttel kell rögzíteni, mert pénzkezelési bug lehet.
Live mode credential hiánynál csak warning van: backend/src/trading/orchestrator.rs (line 800). A UI/bot állapot nem kap egyértelmű hibát.
Nagy helyi diff: 24 fájl, 370 insert, 1367 delete. Javítás előtt érdemes külön branch/commit határt húzni.
Plan
A cél először a minőségi kapuk zöldítése, utána a tényleges trading-flow kockázatok javítása. A sorrend azért ez, mert a build és teszt már zöld, de a lint/clippy zaj jelenleg elfedi a valódi production hibákat.

Scope
In: backend clippy/lint hibák, frontend any warningok, paper/live trade logika validálása, SSE latency warning, credential/order failure jelzés.
Out: új stratégia, UI redesign, Binance/Polymarket API architektúra nagy átírása.
Action items
[ ] Védd le a jelenlegi dirty állapotot: git status, majd külön branch vagy checkpoint commit, mielőtt bármi javítás indul.

[ ] Javítsd az egyszerű backend Clippy hibákat: side: side → side, map_or → is_ok_and / is_none_or, arr.get(0) → arr.first(), range check → (a..=b).contains().

[ ] Javítsd az SSE latency warningot backend/src/api/sse.rs (line 513): ne legyen felesleges kezdőérték, a price fetch ágak adjanak vissza (yes, no, latency) tuple-t.

[ ] Kezeld külön a strukturális Clippy hibákat: too_many_arguments esetekhez vezess be kis request/config structokat a DB query rétegben, de csak ott, ahol több hívó is ugyanazt a paraméterhalmazt adja tovább.

[ ] Implementáld vagy nevezd át a félrevezető trait-szerű metódusokat: strategy default() metódusoknál impl Default, PositionSide::from_str esetén impl FromStr.

[ ] Adj célzott backend tesztet a paper flow-ra: egy paper trade signal után pontosan egy trade_decision rekord jöjjön létre, settlementkor pedig ne duplikálódjon a döntés.

[ ] Rögzítsd teszttel az order sizing szerződést: bet_size USDC amount vagy share size. Utána igazítsd össze az orchestrator és quick trade OrderRequest.size logikáját.

[ ] Live credential hiánynál küldj BotEvent::Error eseményt és növeld a bot hibaszámlálóját, hogy a dashboard ne csak csendben “running” állapotot mutasson order nélkül.

[ ] Frontenden vezess be minimális EIP-1193 provider típust a MetaMask hívásokhoz, és cseréld az err: any kezelést unknown + type guard/helper alapú kezelésre.

[ ] Futtasd újra: cargo test, cargo build --release, cargo clippy --all-targets -- -D warnings, cd frontend && bun run lint, cd frontend && bun run build.

